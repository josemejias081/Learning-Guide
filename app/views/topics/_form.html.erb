<section class="section-form">
  <div class="card card-form">
    <div class="card-body">
      <%= form_with(model: @topic) do |f| %>
      <% if @topic.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@topic.errors.count, "error") %> prohibited this topic from being saved:</h2>
          <ul>
            <% @topic.errors.each do |error| %>
              <li><%= error.full_message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>
      <div class="form-group">
        <%= f.label :nombre %>
        <%= f.text_field :name,class: 'form-control' %>
      </div>

      <div class="form-group">
        <%= f.label :descripción %>
        <%= f.text_field :description,class: 'form-control' %>
      </div>

      <div class="form-group">
        <%= f.label :link %>
        <%= f.text_field :link,class: 'form-control' %>
      </div>

      <div class="form-row">
        <div class="form-group col-md-12">
          <label>categorias</label><br>
          <div class="text-dark">
            <%= f.select :category_ids, options_from_collection_for_select(@categories, :id, :name, @topic.categories.pluck(:id)), {}, multiple: true, id: "category_ids", class: "form-control select-exercise"%>
          </div>
        </div>
      </div>

      <div class="form-group mt-2">
        <%= f.label :nota %>
        <%= f.rich_text_area :note,class: 'form-control' %>
      </div>

      <div>
        <%= f.submit "Guardar", class:"btn btn-success mt-3" %>
      </div>

    <% end %>
      
    </div>
  </div>
</section>
